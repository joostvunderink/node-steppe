var should = require('should');

var Marble = require('../lib/marble');

describe('bla', function() {
  it('should bla', function() {
    var marble = new Marble();
    console.info(marble);
    var x = 'bla';
    x.should.equal('bla');
  });
});